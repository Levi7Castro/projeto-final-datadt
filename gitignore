# 1. Arquivos de Ambiente Virtual Python
.venv/
venv/
env/

# 2. Arquivos de Cache e Compilação Python
__pycache__/
*.pyc

# 3. Diretórios de Log e Cache do Apache Airflow (CRÍTICO!)
airflow/logs/
airflow/airflow.db  # O banco de dados SQLite padrão do Airflow (se estiver usando-o)
airflow/logs/__pycache__/

# 5. Arquivos de Chaves/Configurações Sensíveis (SENHAS, TOKENS, etc.)
.env
